# Investment Dashboard — 项目工作体系（PROJECT_CONTEXT）

> 本文件是项目的"认知层"文档，供 Agent 每次接手本项目时加载。
> 它回答 8 个问题：这是什么项目、目标是什么、用户怎么用、投资体系是什么、技术怎么搭、现在有什么问题、长期要走向哪里、Agent 在这里该怎么工作。
> 代码变化导致本文件过时时应主动更新（规则见全局 AGENTS.md）。

---

## 1. 项目认知

**一句话**：一个基于"供应链瓶颈逆向映射框架"的个人基金投资监测仪表盘，用领先指标提前 3-12 个月判断行业方向，而不是追涨杀跌。

- 类型：个人单用户 Web 应用（Flask + 原生 JS），研究/教育用途，非投资建议。
- 使用者：本人（ODM 公司 BSP 工程师，非全职投资者），持有 A 股 + QDII 场外基金，无法盘中交易（T+1 确认）。
- 方法论来源：Serenity（@aleabroreddit）的供应链瓶颈逆向映射框架；项目内已内置 `serenity-bottleneck-hunter` skill 作为方法论文档。
- 核心理念：**领先于价格，不追随价格**。盯的是"瓶颈在收紧还是缓解"（设备订单、CapEx、产能利用率、合约价等），不是股价涨跌。
- 板块范围：9 个监控板块——半导体设备、CPO 光模块、海外半导体、电子材料、军工电子、电网设备、机器人、存储芯片、芯片指数（仅监测无持仓）。
- 关键约束：基金视角而非股票视角——日涨跌几乎不触发操作；C 类份额 <7 天有 1.5% 惩罚费；份额（shares）是持仓的源头真相。
- 仓库：GitHub `Cming592679/investment-dashboard`，分支 `main`，2026-07-07 上线，7 个 commit。
- 个人数据分离：真实持仓/历史/预测/复盘存在外部 `PERSONAL_DATA_DIR`（由 `.env.local` 指定，指向个人数据目录），**绝不进入 Git**。

---

## 2. 产品目标

### 核心目标

做一个**前置预警系统**：不是告诉你"已经发生了什么"，而是告诉你"接下来可能发生什么"。

1. 对每个板块给出明确结论：🟢 安心持有 / 🟡 忍着不动 / 🔴 考虑跑路。
2. 每个结论附带**可验证、可证伪**的预测（方向 + 时间框架 + 验证日 + 证伪条件）。
3. 基于产业链瓶颈分析，不是基于价格图表。
4. 每周看一两次就够，不需要每天盯盘。
5. 每周/每月复盘，持续校准权重与阈值。

### 设计原则

- **领先于价格**：指标选"还没反映在股价里"的变量（领先股价 3-12 个月）。
- **瓶颈驱动**：每个指标可追溯到产业链瓶颈点（标注瓶颈原型编号）。
- **基金视角**：日涨跌降权到近乎忽略，只有系统性恐慌（≥3 家 >8%）才扣分。
- **可验证**：预测方向明确，错了就认，用双轨验证（Tier-1 指标层 / Tier-2 价格层）。
- **诚实优于好看**：置信度标"低"也不假装确定。

### 明确不做的事

- 不做个股短线交易建议（用户买的是场外基金）。
- 不做自动交易、不做投资决策替代（用户保持独立判断）。
- 不追求"评分好看"——决策树结论优先于线性分数展示。

---

## 3. 用户工作流

### 用户画像

- 非全职投资者：希望每周看 1-2 次，而不是每天盯盘。
- 用户原话："手续费不重要，基本面判断才重要"；"每日独立判断，不因手续费限制操作"。
- 心力有限（工作繁忙）：系统应给出明确结论 + 少量必看信息，不制造噪音。
- 自己保持独立判断：系统结论是参考，用户决定是否执行（如结构性卖出可能不执行）。

### 日常使用流程

1. **自动运行**：18:00 保存当日板块快照；20:00 拉取收盘净值更新持仓金额；盘中（通过 apizero API）估算涨跌与告警。
2. **每周 1-2 次**：打开仪表盘看板块结论（🟢/🟡/🔴）、领先指标是否过期（健康圆点）、关键事件倒计时。
3. **想问"能不能买/该不该加仓"**：走 `.claude/skills/technical-check.md` 的五项入场检查（MACD / 布林 / KDJ / RSI / MA50），必须带统计数据。
4. **交易后**：用 `record_trade.py` 记录（buy/sell/plan），保持 shares/cost/pending_plans 同步。
5. **财报/数据发布后**：手动更新 `config.py` 中对应领先指标的 value/trend（这是刻意设计——逼自己读行业动态）。
6. **每月 1 日**：生成月度复盘报告（Tier-1/Tier-2 验证、偏差分析、校准建议）。
7. **观察待执行计划**：`portfolio.json` 的 `pending_plans` 由 `intraday_check.py` 检查触发条件（如军工电子 -15% 减仓计划、CPO 加权 RSI<50 加仓计划）。

---

## 4. 投资体系

### 4.1 分析框架：供应链瓶颈逆向映射

每个板块从下游到上游拆 5 层（Layer5 需求 → Layer4 系统 → Layer3 器件 → Layer2 设备 → Layer1 材料），猎物集中在 Layer 1-2（设备和材料）。用 6 个瓶颈原型标注每个领先指标：

① 材料/衬底垄断 · ② 单一来源卡脖子 · ③ 产能售罄/锁定 · ④ BOM 普适 · ⑥ 设备/测试瓶颈 · ⑧ 巨头依赖护城河 · ⑨ 宏观二阶/政府预算

### 4.2 仪表盘评分（展示层）

`综合评分 = 领先指标(0-5) + 周期位置(0-2) + 技术面(0-3)`，10 分制：🟢≥6 / 🟡3-5 / 🔴0-2。**分数只用于展示，操作判定走决策树/交易规则层。**

### 4.3 叠层信号交易系统（规则层，trading_rules.py）

- **L1 技术面 ×1.0**：RSI / MACD / KDJ / Bollinger / MA50 / MA20-MA60 / 量价关系（放量上涨=真突破，缩量反弹=死猫跳）。
- **L2 瓶颈面 ×2.0**：领先指标加权趋势（瓶颈关键指标权重更高）+ 瓶颈破坏条件 + 联动升降级。**L2 负分 → 所有买入作废。**
- **L3 周期面 ×1.5**：early +2 / mid +1 / mid-to-late -1 / late -2，红色风险额外 -1。
- **L4 事件面 ×1.0**：近 14 天关键事件结果（关键词匹配，上限 ±4）。

### 4.4 Regime 体制判定（板块级）

| Regime | 触发 | 买入 | 卖出 |
|--------|------|------|------|
| 🟢 Normal | 默认 | 全额 | 全额 |
| 🟡 Panic | 基准ETF单日跌>5% 或 RSI<30 成分≥3 | 减半 | **记录不执行**（恐慌不割肉） |
| 🔴 Structural | L2 瓶颈破坏 negative | **禁止** | ×1.5 |
| ⚪ Pre-Event | 48h 内 critical 事件 | 冻结 | 冻结 |

### 4.5 买卖规则

- **买入**：叠层得分≥阈值 + L2 非负 + MA20>MA60 趋势过滤器（≥50% 成分股）+ RSI 分级加仓系数（45-40→×0.3 … <30→×1.0）+ 信心系数（取较小者）。
- **卖出双轨**：
  - 止盈轨道 A（目标止盈，按周期档位 10~60%）、B（技术过热：RSI+KDJ 双热 / 布林上轨+MACD 死叉）、C（时间止盈：6 个月无效仓位标记——暂因缺买入日期不完整）。
  - 止损轨道 D（**唯一的止损，无价格止损**）：1-2 个领先指标转 down → 减 30%；瓶颈破坏 negative → 减 50%；≥2 down + 瓶颈破坏 → 清仓。
- **仓位硬约束**：标准仓 10%、板块上限 20%、单基金上限 15%、现金下限 10%、持仓 5-8 只。
- **冲突消解**：硬约束 > L2 一票否决 > Regime 调制 > 同向取最大值 > 异向卖出优先。

### 4.6 双轨验证与复盘

- **Tier-1 指标层**（季度验证）：预测的指标趋势 vs 实际公布数据——检验方法论对错（需 ≥90 天样本才开启，当前未通过门禁）。
- **Tier-2 价格层**（7-30 天）：方向+强度三档判定（✅/➡️/❌），置信度加权（高×3/中×2/低×1）。
- **偏差分析**：乐观/悲观偏差、板块级偏差、信号衰减检测；对照组（买入持有 vs 系统研判）对比超额收益。

### 4.7 关键配置位置

全部在 `config.py`：FUNDS（板块/成分股/阈值）、LEADING_INDICATORS（领先指标，手动维护 trend/value）、KEY_DATES（关键事件+result）、CYCLE_ASSESSMENTS（周期）、BOTTLENECK_DISRUPTION（瓶颈破坏条件）、SHARED_INDICATORS（共享指标联动）、TRADING_CONFIG（规则引擎全部参数）、HOLDING_WEIGHTS（成分股权重）。

---

## 5. 技术架构

### 5.1 总体形态

单进程 Flask 应用 + JSON 文件存储（无数据库服务器），前端原生 JS + 单 CSS。Python 3（`/usr/lib/google-cloud-sdk/platform/bundledpythonunix/bin/python3` 或项目 `.venv`），依赖仅 `flask`、`yfinance`。

```
investment-dashboard/
├── app.py              # Flask 主入口：路由/API/缓存/决策树/健康检测/复盘生成/调度器（~1850行）
├── config.py           # 全部配置：基金/领先指标/周期/事件/瓶颈破坏/交易规则参数（~1365行）
├── data_fetcher.py     # yfinance 封装：股价/RSI/MACD/KDJ/布林/均线/量能（retry+NaN防护）
├── fund_nav_fetcher.py # 新浪(收盘净值)+东方财富(历史)+apizero(盘中估值)：份额驱动更新
├── trading_rules.py    # 叠层信号规则引擎：Regime/L1-L4/止盈止损/冲突消解（~980行）
├── position_engine.py  # 动态仓位模型 V2：RSI/趋势/基本面/波动率 四因子
├── intraday_check.py   # 盘中检查：pending_plans 触发状态 + 异常告警
├── record_trade.py     # 统一交易记录入口（buy/sell/plan）
├── templates/          # index.html(基金监控) + portfolio.html(仓位总览)
├── static/style.css
├── fund_weights.json   # 部分板块成分股权重（用于加权 RSI）
├── optimize/topics.json# 优化专题清单
├── run.sh / .env.local # 启动与环境变量（PERSONAL_DATA_DIR）
└── .claude/skills/     # serenity-bottleneck-hunter + technical-check.md
```

### 5.2 数据层（JSON 文件，位于 PERSONAL_DATA_DIR，不入 Git）

- `portfolio.json`：持仓（shares/cost_basis/amount/status/tier/notes）+ 现金 + 板块分配 + action_log + pending_plans。
- `history/`：每日板块快照（18:00）+ `_index.json`。
- `predictions/`：每日预测记录（方向/置信度/验证日/indicator_predictions）。
- `reviews/`：月度复盘报告（markdown）。
- `portfolio_history/`：持仓净值历史（20:00 更新）。
- `position_snapshots/`：仓位模型快照（position_engine --save）。

### 5.3 核心机制

- **缓存**：板块数据内存缓存 + 启动后台预热；`?refresh=1` 强制刷新。
- **调度器**：`_scheduler_loop`（18:00 快照、20:00 净值、整点缓存刷新）。
- **健康检测**：领先指标按 update_cycle 分级判过期 + 关键事件遗漏检测（`/api/health`）。
- **决策树**：8 分支判定（量能分支、联动降级、瓶颈破坏、背离降级等），结论字符串驱动前端颜色（分数已弃用作判定）。
- **复盘生成**：Tier-1 门禁（≥90 天）+ Tier-2 动态阈值 + 置信度校准 + 对照组超额收益。
- **API**：`/api/funds`、`/api/fund/<id>`、`/api/refresh`、`/api/portfolio`、`/api/action`、`/api/intraday`、`/api/history`、`/api/health`、`/api/review[/generate]`、`/api/bottleneck-exposure`、`/api/optimize/topics`。

### 5.4 数据源

- Yahoo Finance（yfinance）：股价 + 技术指标，A 股偶发失败（已加 retry 3 次 + 指数退避）。
- 新浪财经：基金收盘净值；东方财富：历史净值；apizero：盘中实时估值（免费匿名 50 次/天，注意限额）。

### 5.5 已知架构特征（可改进点）

- `app.py` + `config.py` 两个大文件承担过多职责，模块边界靠注释维持。
- 无用户认证（仅 localhost，单用户场景可接受）。
- 测试：`tests/`（unittest，决策树/规则/divergence/验证闭环）+ GitHub Actions CI；验证同时靠 `/api/health`。
- 前端无构建步骤（原生 JS），无类型检查。

---

## 6. 当前问题

> 按严重程度排序。2026-07 复盘显示系统性问题，务必正视。

### 6.1 预测准确性严重不达标（最高优先级）

- 2026-07 首次 Tier-2 验证：**35 条预测中 ✅ 仅 5 条、➡️ 4 条、❌ 26 条**。7 月市场下跌期间系统持续看涨，出现明显的**乐观偏差**。
- 置信度校准倒挂：中置信度准确率 2.6% < 低置信度 6.6%——置信度体系未能区分把握大小，需要重构或暂不采信。
- 对照组显示部分板块超额收益为负（如半导体设备累计 -41.86% vs 基准 -2.44%），需回答"系统是否提供了超越 beta 的价值"。
- Tier-1 门禁未通过（16/90 天），方法论对错尚无有效证据；**首次权重校准至少需积累一个季度真实数据（约 2026-10）**。

### 6.2 持仓层面问题（个人数据，仅描述不列数字）

- 军工电子仓位超限（>20% 板块上限），notes 已计划等 RKLB 财报后反弹减仓。
- 海外半导体持仓已清仓（sold），相关配置保留仅供复盘。
- 部分持仓持有收益为负且幅度较大；`tier: overweight` 的仓位需持续跟踪。

### 6.3 数据维护负担

- 领先指标依赖手动更新（`config.py`），忘记更新会导致基于过期数据的结论；健康检测只能"提醒"不能"更新"。
- 北向资金、两市成交额、融资融券、宏观层（PMI/社融/LPR/CNY）、基本面估值（PE/PB/ROE）均未接入。
- yfinance 对 A 股数据偶发失败；akshare 备用源被推迟（optimize topic #2 已 done，但未真正接入）。

### 6.4 方法论与实现缺口

- 无历史回测（2023-2026）；评分权重基于逻辑推理而非统计优化。
- 无板块相关性矩阵——半导体设备/芯片指数/存储高度重叠，20% 板块上限可能形同虚设。
- 轨道 C（时间止盈）因持仓缺买入日期而不完整。
- L4 事件面用关键词匹配结果，可能误判（如"营收超预期但利润 miss"）。
- 瓶颈破坏信号可能过度敏感（如 CXMT HBM3 触发 AI 硬件 Structural，但技术差距仍有 2-3 年）。
- 无用户认证、无测试、无 CI（单用户可接受，但重构风险高）。

### 6.5 优化专题队列（optimize/topics.json）

- pending：决策树结论验证校准（等 7/13 首批预测到期后对比，原"逃跑指数"已随线性分数弃用）、验证周期分层准确性（等 2026-10）、共享瓶颈敞口加权计算（需持仓金额）、增加 MA20/MA50 金叉死叉辅助信号。
- done：A 股数据源稳定性（retry）、启动缓存预热。

---

## 7. 长期目标

### 系统层面

1. **跑满验证闭环**：积累 ≥90 天 Tier-1 样本，通过门禁后做第一次权重/阈值校准（目标 2026-10）。
2. **回测验证**：用 2023-2026 历史数据模拟系统研判 vs 实际走势，检验权重合理性。
3. **从"看对方向"到"贡献超额收益"**：对照组持续为正，证明系统价值高于简单买入持有。
4. **补齐数据层**：akshare/东方财富备用源、宏观数据、估值体系、北向/两融。
5. **体系化监控瓶颈替代方案**（纳米压印、中国 EUV 进展等），防止瓶颈假设过时。

### 用户层面

- 让用户比 90% 的散户更理性：强迫系统性追踪领先指标、记录预测、诚实面对偏差。
- 作为"AI + 行业"方向的实践作品，为长期职业目标（可积累技能、有作品/定价权）提供真实案例。

### 非目标

- 不做自动化交易系统；不做公开产品；不追求复杂化（保持单用户工具的简单性）。

---

## 8. Agent 工作原则（本项目专属）

优先级：系统安全 > 用户明确要求 > 项目规则（本文件 + AGENTS.md）> 全局 AGENTS.md > 默认判断。

### 8.1 数据与隐私红线

- **绝不提交个人数据**：`portfolio.json`、`history/`、`predictions/`、`reviews/`、`portfolio_history/`、`position_snapshots/`、`.env.local` 均在 .gitignore，严禁 git add 这些路径。
- 报告中避免展示具体持仓金额/累计盈亏（除非用户要求）；需要说明问题时用"超限/深亏/已清仓"等定性描述。
- 不向仓库写入真实 API key、token、个人路径。

### 8.2 任务前必查

- `git status` + 区分用户已有修改；本项目仓库干净时正常 commit/push（远程 origin/main）。
- 涉及个人数据目录的操作（PERSONAL_DATA_DIR）只读优先；写个人数据前与用户确认。

### 8.3 修改 config.py 的纪律

- 领先指标 trend 必须诚实：up/flat/down 依据真实数据；没有数据用 `"--"`，不猜。
- 更新后同步 `update_cycle`；关键事件加 `result` 字段时写清楚"超预期/不及预期"的判定依据。
- 周期判断（CYCLE_ASSESSMENTS）标注 counter_hypothesis；瓶颈破坏条件标注 affected_funds + positive_for。

### 8.4 给出买卖建议时

- 必须走 `.claude/skills/technical-check.md` 的五项入场检查流程，列出完整检查表 + API 统计数据，禁止"感觉可以"。
- 方法论问题优先参考 `serenity-bottleneck-hunter` skill 与 DESIGN.md。
- 结论必须反映"基金视角"：不因单日涨跌给操作建议。

### 8.5 验证与提交

- 代码改动验证：`python3 -m py_compile` 相关模块 + 启动冒烟（/api/health、/api/funds）+ 关键 API 返回结构检查。
- commit 遵循 Conventional Commits（如 `feat: ...` / `fix: ...` / `docs: ...`），原子提交，正常 push。
- 完成任务后按全局规范报告：Changes / Validation / Commits / Push / Working Tree / Known Issues。

### 8.6 文档维护

- README / DESIGN / TRADING_METHODOLOGY / REVIEW_METHODOLOGY 与本文件构成项目文档体系；代码行为变化时同步更新对应文档。
- 本文件（PROJECT_CONTEXT.md）随项目演变持续更新，保持"当前真实状态"而非历史快照。

---

> 最后：这个系统的价值不在于"算出一个对的分数"，而在于强迫自己系统性地追踪领先指标、记录预测、诚实面对偏差。即使评分不准，这个过程本身就能让你比 90% 的散户更理性。
