# Investment Dashboard — 全面审计报告

> 审计日期：2026-08-17 · 范围：全项目只读审计（代码、配置、文档、Git 历史、个人数据目录结构与运行数据）
> 审计方式：通读全部 Python 模块（app.py / config.py / trading_rules.py / data_fetcher.py / fund_nav_fetcher.py / position_engine.py / intraday_check.py / record_trade.py）、两个页面模板、样式表、四份文档、7 个 commit、持仓与预测/复盘数据。
> 本报告不修改任何代码。所有金额类个人数据已做定性化处理，不落盘具体数字。

---

## 1. 产品定位审计

**定位正确性：高。** 系统解决的是真实痛点：场外基金投资者看盘滞后、无法盘中交易、容易追涨杀跌。

- 核心理念（领先于价格、瓶颈驱动、基金视角、可证伪）自洽且差异化，不是"又一个行情面板"。
- "日涨跌不触发操作"与场外基金 T+1、C 类赎回费约束匹配——这是系统最重要的设计正确性。
- 定位为个人研究工具而非产品，与当前规模匹配。

**问题：**

1. 产品叙事与实现已出现分层：README 写"8 分支决策树"，但决策树实现有分支结构问题（见 §7），且线性评分（score）已弃用却仍在复盘/预测/UI 中残留，产生"展示系统与判定系统不一致"。
2. "芯片指数"（014194）已裁剪出持仓，只作监测——README 仍把它列为监控板块之一，功能上它现在只是温度计，产品定位未明确区分"持仓板块 / 观测板块"。
3. "预测准确率达标"是系统存在的前提，但首次验证显示不达标（见 §7），当前产品定位需要先在"方法论有效"上自证，否则仪表盘只是"好看的看板"。

---

## 2. 用户工作流审计

**设计意图：每周 1-2 次查看，自动收集，手动更新领先指标。** 与用户"非全职、心力有限"的现实匹配。

**实际工作流中的断点：**

1. **买入决策有两条路，结论可能冲突**：用户在对话中用 technical-check skill（五项入场检查），系统内另有 /api/action（叠层信号）+ position_engine（仓位模型）。同一"能不能买"问题有三种答案来源，且没有统一仲裁。
2. **交易记录入口单一且脆弱**：record_trade.py CLI 读取 DATA_DIR 的 portfolio.json，却写入当前目录的 portfolio.json（见 §6）——若从项目根目录运行，交易记录不会进入个人数据目录，存在静默丢失风险。
3. **领先指标手动更新缺少"待办"视图**：健康检测能提示过期，但没有一个"该去更新哪些指标、上次更新时间、卡在哪个事件"的聚合清单。
4. **复盘报告不可在 UI 阅读**：优化面板只显示复盘文件名，点击无动作，用户仍需进文件系统。
5. **待执行计划分散**：pending_plans 存在 portfolio.json，触发状态在仓位页展示，但与"今日操作建议"（/api/action）是两个独立区块，用户需要自己拼接。

---

## 3. UI/UX 审计

**整体：信息密度高、结论显眼、深色主题一致性好；作为单用户工具达到可用水平。**

**问题（按影响排序）：**

1. **"逃跑指数"标签过时**：决策树时代已无指数（score 恒为 0），但 index.html 仍显示"逃跑指数"标签并把 emoji 放进数字位——历史遗留 UI，误导性最大。
2. **复盘行不可点击**（portfolio.html `renderReviews` 只渲染文件名）：期望是点击查看全文。
3. **历史快照 chip 仍显示 score/10**：决策树重构后 score 恒为 0，未来所有 chip 都会显示 0/10。
4. **操作建议区域缺"为什么被否决"的完整上下文**：conflicts_resolved 有折叠展示，但 buy/sell 卡片不显示对应 Regime 与 L2 明细，用户无法判断信号可信度。
5. **无全局错误态**：数据源失败时渲染 '--' 或"错误"，没有统一的重试/降级提示；刷新按钮点击后只有 2 秒等待，随后静默。
6. **标签页圆点颜色依赖结论字符串匹配**（contains 安心/持有/跑路/减仓…）：新增结论文案会静默回退到黄色，脆弱。
7. 无移动端专项适配（可用但拥挤）、无键盘可达性/aria、无深浅色切换——个人工具可接受，记录待办。

---

## 4. 信息架构审计

**现状：三页结构（基金监控标签页 / 仓位总览 / 优化面板），层次合理。**

**缺口：**

1. **没有"板块时间线"视图**：评分/结论/实际走势的历史只有复盘报告里有，页面本身无任何趋势可视化。
2. **没有主题敞口视图**：position_config 定义了半导体 35% / 军工 25% 等主题上限，但 UI 只显示板块分配（>20% 警告），主题级风险不可见（实际已超限，见 §8）。
3. **今日操作建议藏在仓位页**：用户第一落点是基金监控页，但最重要的"今天该干嘛"在第二页。
4. **复盘/预测/快照三个历史体系各自独立**：predictions/、history/、portfolio_history/ 三个目录互不关联，没有统一时间线索引。
5. 没有全局搜索或"最近发生了什么"摘要（如：今日触发的信号、明日事件、过期指标数）。

---

## 5. 技术架构审计

**形态：单进程 Flask + JSON 文件存储 + 原生 JS，依赖仅 flask/yfinance。与单用户场景匹配，简单是其优点。**

**问题（按严重度）：**

1. **app.py（1848 行）+ config.py（1365 行）双巨石**：路由、调度、复盘、决策树、数据存取全部在一个文件；config 同时是"数据仓库"和"规则引擎参数"，变更风险高、无法独立测试。
2. **零测试、零 CI、零类型检查**：核心是投资决策逻辑（决策树 + 规则引擎），却是全项目最不可测试的部分；任何阈值改动都无回归保障。
3. **并发写无保护**：调度器线程 20:00 写 portfolio.json，同时 API（/api/portfolio/update、/api/portfolio/action）可写同一文件，无文件锁/单写者——存在丢失更新风险。
4. **api/portfolio/update 无校验**：`pf.update(request.json)` 直接合并任意 JSON，一条错误请求可破坏持仓数据结构。
5. **异常处理靠 print + 静默吞掉**：调度器/抓取异常只有控制台输出，无日志文件、无错误计数；运行中故障事后不可查。
6. **依赖未锁版本**：requirements.txt 只写包名；yfinance 行为变化即可破坏指标计算。
7. **debug=True 常驻**（localhost 单用户风险低，但属安全卫生问题）。
8. **外部数据源四个**（yfinance/新浪/东财/apizero），仅 yfinance 有重试；任一源改版即功能降级。

---

## 6. 数据模型审计

**现状：JSON 文件即数据库，个人数据在 PERSONAL_DATA_DIR，代码/数据分离做得好（.env.local + gitignore）。**

**问题：**

1. **record_trade.py 读写路径不一致（P1 数据丢失风险）**：读取 `DATA_DIR/portfolio.json`，但 CLI 保存写 `./portfolio.json`（当前目录）。从项目根目录执行时，交易记录写入仓库目录（被 gitignore 掩盖），真实个人数据文件未更新——静默分叉。
2. **portfolio.json 无 schema/版本字段**：holdings 字段集不一致（部分有 base/max/theme，部分没有；sold 条目缺 tier；dashboard_id 可空），任何消费方都要做防御式取值。
3. **状态与层级概念重叠**：`status`（active/core/watch/overweight/sold/sell_pending/non_investment）与 `tier`（同语义集合）并存且出现矛盾（如 100055 status=core 但 tier=active）——两套字段表达同一件事。
4. **派生数据重复落盘**：sector_allocation 同时由 fund_nav_fetcher 与 record_trade 重算，逻辑略有差异（卖出/待交割口径不同），存在不一致源。
5. **重复记录**：action_log 出现同一天完全重复的两条"六方向加仓"；pending_plans 同样有重复条目——数据录入无去重。
6. **两个"权重真相"**：`fund_weights.json`（仓库内）与 config.py `HOLDING_WEIGHTS` 内容重复，且前者无任何代码引用（死数据）；BOARD_FUND_MAP 中 STORAGE 只映射到 A 股基金 025209，海外存储 100055 也挂在同一 dashboard id，加权口径混用。
7. **trade_rules 是死数据**：portfolio.json 内完整的手工交易规则（成本结构/最小可行收益/止盈/入场清单）无任何代码读取，与 config.py TRADING_CONFIG 并存且参数不重叠——两套"投资规则"互不知晓（见 §7）。
8. 无备份机制：个人数据目录不在任何版本控制/备份之下，一次误写/磁盘故障即全部丢失（风险控制最薄弱点之一）。

---

## 7. 投资逻辑审计

**方法论方向正确，但落地存在系统性缺陷。**

### 7.1 验证结果不支持"系统有效"（最严重）

- 2026-07 首次 Tier-2 验证：35 条预测 **✅5 / ➡️4 / ❌26**；7 月下跌中系统持续看涨，存在显著**乐观偏差**。
- 置信度校准**倒挂**：中置信度原始准确率 2.6% < 低置信度 6.6%——置信度不度量把握，反而反向；该指标目前无信息量。
- 对照组部分板块超额收益大幅为负（半导体设备累计远差于基准）——尚未证明系统价值 > 买入持有。
- Tier-1 门禁未过（16/90 天），以上结论仍是小样本，但方向性警示成立：**在验证通过前，系统参数不应被当作"正确的"**。

### 7.2 决策树结构缺陷（P1 逻辑问题）

`compute_assessment` 的分支链存在两个结构问题：

1. **量能分支独立于主分支链，且优先于"安心持有/高位警惕/背离观望/正常状态"**：只要量能信号明确（≥40% 成分股同型），主分支 4-8 根本不会执行——结论被量能主导，与文档描述的 8 分支设计不符。
2. **周期中后期暴跌漏判**：若板块"领先指标向好 + 单日暴跌 + MA50 破位 + 非早期 + 量能不明 + 未深度超卖"，会一路落到分支 8 → **"安心持有"**。即最需要警惕的"基本面好但价格已坏"场景可能给出绿牌。

### 7.3 四套规则体系并存，无统一仲裁

| 体系 | 位置 | 输出 | 现状 |
|---|---|---|---|
| 仪表盘决策树 | app.py | 🟢/🟡/🔴 结论 | 驱动 UI，但 score=0 |
| 叠层信号交易系统 | trading_rules.py + TRADING_CONFIG | 买卖金额 | /api/action |
| 动态仓位模型 | position_engine.py + position_config | 目标仓位 | 仅 CLI，UI 不展示 |
| 手工交易规则 | portfolio.json trade_rules | 成本/止盈/入场清单 | 无代码读取 |

同一时刻可能给出互相矛盾的"买/不买/持有"，用户承担了系统设计本应解决的仲裁成本。

### 7.4 参数与实现粗糙点

- `_generate_prediction` 使用已弃用的 `assessment["score"]`（恒 0）：watchpoint"评分跌破 3 跑路"永远被追加、永不失效；方向判定实际不受 score 影响。
- L4 事件面关键词匹配（"超预期/beat/miss"）可能误判"营收超预期但利润 miss"。
- 止损轨道 D 的指标恶化数用 `down_weighted // 2` 反推，权重与指标数混为一谈。
- 周期判断（early/mid/late）全手动、主观，counter_hypothesis 只展示不参与评分。
- 置信度校准用固定 ±1.0 阈值，而复盘主表用动态阈值——两处判定不一致。
- 复盘"各板块评分走势"读 `fs['score']`，决策树重构后新快照恒为 0/10，表格将失去意义。

---

## 8. 仓位体系审计

**设计（标准仓 10% / 板块 20% / 单基金 15% / 现金下限 10% / 持仓 5-8 只）合理，但执行与展示脱节。**

1. **军工电子板块 24.6% > 20% 上限**：pending_plan 计划"反弹至 -15% 内或指标转 down 再减"，即系统允许"等待条件"而非立即执行硬约束——纪律被条件化。
2. **半导体主题敞口约 38% > 35% 上限**（设备 + A股存储 + 海外存储 + 全链观察仓）：主题超限仅 position_engine CLI 会告警，页面不展示，用户无感知。
3. **三套仓位参数并存**：TRADING_CONFIG.position（板块 20%）vs position_config.themes（主题 35%/25%/15%）vs trade_rules（成本与止盈），互不联动；板块上限与主题上限可能同时被突破且无仲裁。
4. **仓位模型未接入决策流**：position_engine 因子（RSI/趋势/基本面/波动率）输出目标仓位，但 /api/action 的买入金额只看叠层得分 + RSI 系数，两套金额建议可能打架。
5. **试探仓制度存在但不统一**：watch 档（base 3%/max 5%）在 portfolio.json 有字段，规则引擎未使用该档位。

---

## 9. 交易纪律审计

**用户实际执行 vs 系统纪律：**

- ✅ 执行了结构性卖出（QDII 清仓）、按信号分批操作、记录每笔交易的原因。
- ⚠️ 军工电子已超限（24.6%）仍在 8/10 加仓（"军工续摊"）——超限状态下继续加仓，说明"板块上限"在决策时不是硬约束，而是事后的告警。
- ⚠️ 8/10 六方向同时加仓 8 千元：单日多方向加仓与"标准仓 10%"模型不符（模型会因现金/仓位约束自动收缩，但人工操作绕过了模型）。
- ⚠️ 入场清单（entry_checklist，trade_rules）是人工记忆，没有强制流程；technical-check skill 只在使用时生效。
- ⚠️ 重复记账（action_log 重复条目）说明交易记录流程缺乏唯一标识，长期会污染复盘口径。

结论：**系统纪律存在，但没有被强制执行，也没有在"违反纪律"时显式提示**。用户最需要的不是更多规则，而是"违反规则时系统明确说 NO"。

---

## 10. 风险控制审计

1. **个人数据零备份（P0）**：持仓/预测/复盘唯一副本在 PERSONAL_DATA_DIR，无备份、无版本历史；一次误写即不可恢复。
2. **记录入口写错路径（P0）**：record_trade CLI 的保存目标错误，可能让"以为记了账"变成"没记账"。
3. **数据源配额耗尽（P1）**：apizero 盘中估值免费匿名限额 50 次/天；intraday_check 的 get_estimate 无缓存，每次打开仓位页 run() + check_alerts() 对每只持仓各调一次（约持仓数 ×2），几次刷新即耗尽当日配额，随后盘中功能静默失效。
4. **黑天鹅敞口**：设计上无价格止损，依赖"领先指标先于股价恶化"的假设；该假设未被验证（Tier-1 未过门禁）。
5. **集中度风险**：板块/主题上限形同虚设（见 §8），半导体集群实际敞口远超单一主题上限；无相关性矩阵，存储/设备/芯片高重叠未被量化。
6. **汇率风险**：QDII 持仓收益含汇率波动，系统未区分汇率 vs 资产本身。
7. **无回测**：所有阈值/权重基于推理，未经历史检验；在验证通过前，系统参数本身是最大未量化风险。
8. **并发写与无校验 API**（见 §5/§6）可导致数据损坏。

---

## 11. AI/Agent 介入点审计

**现状：** 两个 skill（serenity-bottleneck-hunter 研究主题候选、technical-check 入场检查）+ CLI 半自动工具 + 手动更新 config。

**当前最值得 AI 介入、但未自动化的环节（按价值排序）：**

1. **领先指标/事件更新助手（最高价值）**：财报/发布会后从原文提取"超预期/不及预期/趋势 up/flat/down"并生成 config.py 补丁建议——这是系统最大的手动负担，也是最容易出错的环节（目前完全依赖人）。
2. **事件结果标注**：替代 L4 关键词匹配，让 AI 判断"营收超预期但利润 miss"这类语义，回写 result 字段。
3. **复盘深度分析**：当前复盘是模板统计；AI 可写"为什么错/偏差归因/建议参数变更（受门禁约束）"。
4. **回测与相关性分析**：用历史数据模拟决策树/规则引擎，输出权重敏感性、板块相关性矩阵。
5. **对话式查询**："为什么半导体是跑路结论"——把 decision tree 的 details 翻译成可追问的推理链（当前 details 已具备基础）。

**红线提醒：** AI 生成的投资结论必须走 technical-check 的统计流程；AI 不直接改 config.py 的 trend/value，只产出补丁由用户确认（或明确授权后写入并 commit）。

---

## 12. 当前最严重的问题（Top 5）

1. **验证显示系统预测不达标且置信度倒挂**（7 月 5✅/4➡️/26❌）——方法论有效性未证明，一切参数都可能是错的。
2. **四套规则体系并存、无统一仲裁**——决策树 / 叠层交易 / 仓位模型 / 手工规则可能互相矛盾，用户被迫自己仲裁。
3. **仓位与主题上限被突破且无强制/无感知**——风控体系是"建议"不是"约束"。
4. **个人数据无备份 + record_trade 写错路径 + 并发写无保护**——数据完整性风险，可能静默丢数据。
5. **决策树存在漏判路径（周期中后期暴跌 → 安心持有）与量能分支权重失真**——最危险的结论可能在最需要警惕时给出。

---

## 13. 可以删除的东西

1. `config.py` 的 `EXIT_LEVELS`（五级线性分级，无代码引用，决策树已替代）。
2. `fund_weights.json`（无代码引用，HOLDING_WEIGHTS 是唯一生效源；删除前确认无外部脚本依赖）。
3. portfolio.json 的 `trade_rules` 字段（无代码读取；内容迁入 config/TRADING_CONFIG 或删除后不再维护）。
4. 决策树重构后的 `score` 展示残留：index.html "逃跑指数"框、history chip 的 score/10、`_generate_prediction` 中 score 相关 watchpoint、复盘评分表（改用结论/决策树路径替代）。
5. 已 sold 的持仓（024239/021169 等）在 holdings 中的冗余字段（保留 sold 记录用于复盘，但清理不一致字段）。
6. action_log / pending_plans 中的重复条目。
7. `024239` 若确定不再入场，可从持仓监控中降级为观测板块（与 014194 同类处理）。

---

## 14. 应该新增的东西

1. **统一规则单一事实源**：一份机器可读的规则配置（合并 TRADING_CONFIG + position_config + trade_rules + 决策树参数），各消费方从它派生，杜绝多源漂移。
2. **强制约束层**：买入/加仓前检查板块与主题上限、现金下限，**超限则硬拒绝并给出理由**（区别于现有"告警"）；用户 override 需要显式记录。
3. **预测自动回填验证**：预测到期后自动从快照取实际值写入 predictions，Tier-2 判定结果可追溯。
4. **复盘/时间线可视化**：板块级评分-结论-实际走势的迷你图；复盘报告在 UI 内阅读。
5. **主题敞口面板**：theme exposure bar + 上限标记（复用仓位页的板块分配组件）。
6. **数据健康中心**：指标过期、持仓一致性（shares×nav≈amount）、规则版本漂移、apizero 配额剩余、备份状态、最近一次调度成功时间。
7. **备份机制**：个人数据目录每日归档（本地副本或私有仓库，含加密选项）。
8. **配置 schema 校验**：portfolio.json / config 结构校验 + 版本字段，损坏时快速失败而不是静默。
9. **AI 更新助手**：事件结果标注、领先指标更新补丁（见 §11）。
10. **回测工具**：用历史快照回放决策树/规则引擎，输出命中率与敏感性（受验证门禁约束，不改正式参数）。

---

## 15. 应该重构的东西

1. **app.py 拆分**：routes / services（评估、复盘、调度）/ storage（JSON 读写+锁）三个模块；config.py 的"数据"与"代码"分离（数据可独立校验与版本化）。
2. **统一评估入口**：decision tree + L1-L4 + 仓位模型合并为一个 `evaluate_all()`，输出单一"今日行动计划"（含被否决信号与原因），UI 只渲染一份。
3. **数据层单写者**：所有 portfolio.json 写入走同一 storage 层（文件锁/原子写），scheduler 与 API 共享。
4. **record_trade.py 修复**：读写统一走 DATA_DIR（顺带增加 dry-run 与回滚提示）。
5. **intraday_check 合并到 fund_nav_fetcher 的缓存体系**：apizero 调用集中、限频、配额统计。
6. **决策树重写为可测试的纯函数**：输入快照 → 输出结论 + 命中分支名；补齐"中后期暴跌"分支；量能分支降为"调节"而非"主导"。
7. **复盘生成器纯函数化**：输入 predictions+snapshots → 输出报告，可单测；修掉 score 残留与置信度阈值不一致。
8. **模板结论渲染改为结构化枚举**：前端用结论 id 而非字符串 contains。

---

## 16. P0 / P1 / P2 优先级

### P0（数据与决策安全，立即）

- 修复 record_trade.py 写路径（可能已存在静默丢账）。
- 个人数据目录备份机制（防单点丢失）。
- 数据层单写者 + 原子写（防并发损坏）。
- 决策树漏判路径修复（中后期暴跌不得给出"安心持有"）。
- 置信度校准/score 残留修复（当前指标误导决策，无法用于校准）。

### P1（一致性，1-2 周内）

- 规则单一事实源 + 强制约束层（板块/主题上限、现金下限硬拒绝）。
- 统一评估入口（合并决策树 + 叠层信号 + 仓位模型为一份行动建议）。
- apizero 配额管理（缓存合并、配额余量展示）。
- portfolio.json schema 校验 + 去重 + tier/status 字段统一。
- UI：复盘可读、历史时间线、主题敞口、移除"逃跑指数"残留。
- API 写接口校验（/api/portfolio/update 白名单字段）。

### P2（增强，一个月内）

- 单元测试 + CI（重点：决策树、规则引擎、复盘生成）。
- 回测工具与相关性矩阵。
- AI 更新助手（事件标注、指标补丁）。
- 日志系统（结构化日志 + 错误计数）替代 print。
- 依赖版本锁定；debug 模式关闭。
- 对话式查询与推理链展示。

---

## 17. 最终系统应该长什么样

一句话：**一个"单一行动建议 + 强制风控 + 自动验证闭环 + AI 辅助维护"的个人投资操作系统**，而不是三个互相独立的看板。

### 形态

```
数据层（唯一事实源，schema 化，单写者，自动备份）
  portfolio / holdings / rules / config / predictions / snapshots
        ↓
评估层（纯函数，可测试，一次调用）
  decision tree + L1-L4 叠层 + 仓位模型 + 硬约束
        ↓ 单一输出
行动层
  📋 今日行动计划：买/卖/持有 + 金额 + 理由 + 被否决信号 + 违反约束提示
        ↓
验证层（自动闭环）
  预测 → 到期回填实际 → Tier-1/Tier-2 判定 → 月度复盘 → 门禁内参数校准
        ↓
AI 介入层
  事件标注 / 指标更新补丁 / 复盘归因 / 回测分析 / 对话式追问
```

### 用户体验

- 打开页面第一眼：**今日行动计划 + 违反纪律的红灯**（如"军工超限，禁止加仓，除非 override"）。
- 其次：每个板块一张卡片 = 结论 + 命中分支 + 3 个关键理由 + 迷你趋势图。
- 再次：健康中心（数据新鲜度/配额/备份/一致性），一眼知道系统"可信度"。
- 每周一次：一份"这周发生了什么、系统说了什么、对不对、下一步"的摘要。
- 每月一次：自动复盘 + AI 归因 + 受门禁约束的参数建议，用户确认后生效。

### 不变的东西

- 单用户本地工具，无数据库服务器，无登录系统（保持简单）。
- 个人数据与代码严格分离，个人数据可备份、可迁移、不泄露。
- 基金视角、领先指标、可证伪——这些设计哲学不动。

> 审计结论：系统骨架和理念是好的，当前最大的敌人不是功能不足，而是**规则分裂、验证未闭环、风控未强制**。优先做 §16 的 P0 项，再做统一行动层，这个系统会从"好看的看板"变成"可信的决策辅助"。
