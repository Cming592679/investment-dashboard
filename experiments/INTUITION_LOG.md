# Intuition Log（方法论 + 模板）

目的：把“我感觉市场变弱了 / 该减仓了”变成可验证记录，而不是系统输入。

每条的验证路径：记录时的客观候选指标 → 预期的市场状态 → 未来 5D/20D 结果 → 与简单基准对比。

```text
Date:
User intuition:
Why user thinks so:
Expected market state:
Expected consequence:
Candidate objective indicators:
Status: UNVERIFIED / VALIDATED / REJECTED
```

完整条目（含个人上下文）维护在 `PERSONAL_DATA_DIR/experiments/INTUITION_LOG.md`，不入 Git。
首批条目：2026-09-07“反弹了，先减点仓”（已归档到个人数据目录）。
