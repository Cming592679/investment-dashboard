# Investment Dashboard v2 — 重新设计方案

> 设计日期：2026-08-17 · 状态：草案 v1（待评审）
> 设计目标（用户原话）：**收益最大化，损失最小化**。
> 本文回答一个问题：如果推倒重来，这个系统应该长什么样，以及为什么。

---

## 0. 设计的第一原则：先定义目标函数

"收益最大化、损失最小化"是一对矛盾，必须显式排序：

1. **损失最小化优先**（硬约束）：账户可生存是第一目标。资金是有限的，一次 -40% 需要 +67% 才能回本；对场外基金 + 非全职投资者来说，尾部风险比错失收益致命得多。
2. **收益最大化其次**（优化目标）：在"不越过风险约束"的前提下，把资金配置到"期望收益 × 置信度"最高的地方。

因此系统的目标函数是：

> **最大化风险调整后收益（Sortino / Calmar），硬约束：最大回撤预算、板块/主题集中度、现金底线、逻辑止损。**

所有组件都必须回答一个问题："它服务于这个目标函数吗？" 不服务的组件（好看的看板、无验证的评分、无意义的日频信号）一律不做。

### 数学上的诚实声明

- 系统**不能保证收益**。它只能保证三件事：① 不犯致命错误（集中押注、拒绝认错、追涨杀跌）；② 减少损耗（赎回费、情绪交易、反复横跳）；③ 只把资金押在**已被验证**的假设上。
- 收益的上限来自"找到并验证 alpha 假设"——这是研究问题，不是工程问题。工程能保证的是：假设没被验证前，它不会悄悄进入你的仓位。
- 对场外基金（T+1、C 类赎回费、无法盘中操作），**仓位与现金管理比择时重要一个数量级**。v2 的决策粒度是"目标权重 + 再平衡"，不是"每日买卖信号"。

---

## 1. 对现有系统的根本判断（为什么要重设计）

审计（AUDIT_2026-08-17）暴露的三个根因：

1. **信号与决策脱节**：系统产出"评分/结论"，但不产出"目标权重"。用户拿到信号后自己决定买多少——最关键的仓位决策完全落在系统之外。
2. **验证未闭环**：预测不自动回填、置信度倒挂、Tier-1 门禁未过。系统在"证明自己有效"之前就已经在影响决策。
3. **风控是告警不是约束**：板块/主题上限被突破仍可加仓；规则分散在四处（决策树 / TRADING_CONFIG / position_config / trade_rules），互不仲裁。

v2 的设计目标不是"更好看的看板"，而是把决策链完整接管：

```
研究（假设） → 验证（门禁） → 组合（目标权重） → 风控（硬约束） → 执行（账本） → 复盘（校准）
```

---

## 2. 设计骨架：决策流水线

### 2.1 五个"端口"，职责隔离

| 端口 | 职责 | 宽松度 | 能否影响仓位 |
|---|---|---|---|
| 研究端口 | 生成 alpha 假设、跟踪事件、维护领先指标 | 宽松、自由探索 | ❌ |
| 验证端口 | 假设的预测→回填→校准→升级/否决 | 严格、机械 | ❌ |
| 组合端口 | 目标权重 = f(已验证假设 × 置信度 × 相关性) | 优化器，无人工 | ✅ |
| 风控端口 | 硬约束检查、熔断、逻辑止损 | 不可 override（或显式记录） | ✅ |
| 执行端口 | 计划单→交易→账本（唯一 ID） | 只记录，不判断 | ✅ |

**核心隔离原则：未验证的假设永远进不了组合权重。** 这直接解决当前"手动领先指标直接进评分"的主观性问题。

### 2.2 决策节奏（与场外基金现实对齐）

| 频率 | 动作 |
|---|---|
| 每日（自动） | 数据刷新、事件监控、逻辑破坏检测——**只告警，不决策** |
| 每周（一次） | 决策会议：假设复核 → 信号确认 → 再平衡建议（目标权重偏离 >3pct 触发） |
| 月度 | 复盘 + 校准（门禁内）+ 假设状态更新 |
| 事件驱动 | 逻辑破坏 / 黑天鹅 / 熔断 → 即时行动（这是唯一的"日频行动"） |

为什么每周而非每日：场外基金 T+1 + 赎回费 + 非全职，日频决策的期望值几乎必然为负（噪音交易成本）。每周一次决策 + 每日自动监控，是信息效率与操作成本的平衡点。

---

## 3. 核心模块设计

### 3.1 Edge Registry（假设登记簿）——系统的"研究记忆"

每个投资逻辑是一条假设，字段：

```yaml
id: edge-storage-2026q3
hypothesis: 存储涨价周期 → 存储板块盈利与估值双击
theme: 半导体集群
leading_indicators:
  - DRAM 合约价 QoQ（季度）
  - 三大原厂 CapEx 指引（季度）
  - 库存周转天数（季度）
falsifiers:
  - DRAM 合约价连续 2 季涨幅 <3%
  - 原厂同时宣布 2028 大规模扩产
  - HBM 出现替代架构商用
created: 2026-07-06
status: experimental      # experimental → active → rejected/superseded
validation_count: 0
max_weight_cap: 0.15      # 未验证时即使 active 也有权重上限
```

状态机：

```
experimental（可研究、不可进组合）
  → 满足门禁（≥3 个独立验证周期 + 校准优于基线）
  → active（可进组合，权重受 cap 约束）
  → 证伪条件触发 / 校准连续 2 期劣于基线
  → rejected（冻结，进入"为什么错了"复盘）
```

### 3.2 组合优化器——系统的"大脑"

**输入：**

- 每个 active 假设的期望收益估计（方向 × 幅度 × 概率，来自验证端口，不是来自观点）
- 置信度（校准曲线给的真实概率，而不是"感觉")
- 板块/主题协方差矩阵（由历史收益计算 + 共享瓶颈修正）
- 硬约束（见风控）

**目标函数：**

```
max  Σ wᵢ·E[rᵢ] − λ·(下行风险) 
s.t. Σ wᵢ = 1 − cash_floor
     单板块 ≤ 15~20%
     主题集群 ≤ 25~30%（相关性/共享瓶颈聚合后）
     单基金 ≤ 15%
     现金 ≥ 10%
     单假设贡献的账户风险 ≤ 风险预算
```

**输出：目标权重向量**，而不是"买/卖金额"。日常执行 = 目标权重与实际权重的再平衡差异。

**为什么这样更接近"收益最大化"：**

- 资金自动流向"期望收益 × 置信度"最高处（高确定性板块重仓，低确定性轻仓）——替代现在的"每板块固定 10%"。
- 再平衡天然实现止盈（涨多了自动减回目标）与纪律性加仓（跌多了自动补回），不需要靠"轨道 A/B"人工触发。
- 现金是默认持仓：没有机会时系统自动满仓现金，而不是硬找机会。

### 3.3 风控覆盖层——系统的"刹车"

**分级硬约束（全部在优化器内部，违反 = 无解，不是告警）：**

1. **结构层**：单板块 ≤ 15-20%；主题集群 ≤ 25-30%；单基金 ≤ 15%；现金 ≥ 10%。
2. **逻辑层**：假设证伪条件触发 → 该板块权重强制归零（无论价格）。这是唯一"无条件"规则。
3. **回撤熔断层**：
   - 账户从峰值回撤 -12% → 现金强制升至 50%，只保留核心假设；
   - -18% → 现金强制升至 80%，进入"防守模式"（除非假设升级为 active 且通过二次校验）。
4. **相关性层**：共享同一瓶颈的板块按"单一敞口"合并计算上限（解决当前半导体集群实际超限问题）。

**熔断不可静默 override**：如需 override，必须在账本中记录理由 + 假设引用，并在复盘中被点名检查。

### 3.4 验证引擎——系统的"裁判"

- 预测格式升级为**三元组**：方向（up/flat/down）× 幅度区间 × 概率（如 60%），配合证伪条件。
- 自动回填：预测到期后从快照自动取实际值写入，产出校准曲线（reliability diagram）。
- 假设升级门禁：≥3 个独立验证周期 + Brier 分数优于基线（如"历史频率"或"买入持有"）→ 才允许 experimental → active。
- 板块/假设级归因：赚钱/亏钱来自哪条假设，验证它的真实贡献（区分 beta 与 alpha）。
- **验证通过前，相关假设 max_weight_cap = 0**——系统默认怀疑一切。

### 3.5 统一账本——系统的"记分牌"

- 每笔交易唯一 ID，必填：原因（引用假设 ID 或风控规则 ID）、金额、净值、份额。
- override 记录独立存放，复盘必查。
- 修复现有 record_trade 写路径问题；所有写入走同一 storage 层（原子写 + 锁 + 备份）。
- 状态模型收敛为一套：`holdings` 只存事实（份额/成本/状态），**权重永远由计算得出，不落盘**（消除 sector_allocation 双写不一致）。

---

## 4. 数据模型（v2）

```
investment-dashboard-v2/
├── rules.yaml               # 单一事实源：目标函数、约束、阈值、费率、门禁参数
├── state/
│   ├── portfolio.json       # 持仓事实（份额/成本/状态）+ 现金
│   └── snapshots/           # 每日快照（含目标权重 vs 实际权重）
├── registry/
│   ├── edges.json           # 假设登记簿（机器读）
│   └── edges.md             # 假设论证（人读）
├── predictions/             # 预测（自动生成 + 自动回填）
├── ledger/
│   ├── trades.json          # 交易账本（唯一 ID）
│   └── overrides.json       # override 记录
├── reviews/                 # 周报/月报（自动生成）
├── assets/                  # 相关性矩阵缓存、校准曲线
└── ai/                      # AI 产出物（事件标注、补丁，需人工确认）
```

全部 JSON/YAML schema 化 + 版本字段；个人数据仍与代码分离，但**增加自动备份**（本地归档 + 可选加密私有仓库）。

---

## 5. 决策流程（用户每周看到的）

### 每周决策会议（10 分钟）

1. **本周发生了什么**：系统自动生成摘要——触发的信号、过期指标、关键事件、风控状态。
2. **组合偏离**：目标权重 vs 实际权重，差异 >3pct 的板块列出再平衡建议（含金额）。
3. **假设状态**：哪条假设到了验证日？结果如何？哪条触发证伪？
4. **今日行动计划**：唯一一份（不是三个页面三套建议）：
   - 🟢 执行：买入/卖出金额 + 理由（引用假设/规则 ID）
   - 🔴 强制：风控动作（熔断/逻辑止损），不可跳过
   - ⚪ 可选：低置信度观察仓（≤5% 合计）

### 用户唯一需要做的

- 确认或 override（override 必须填理由）；
- 每季度花 1-2 小时维护领先指标（或让 AI 生成补丁后确认）；
- 每月读一次复盘（假设对错、参数是否该校准）。

---

## 6. UI/UX（v2 只有三个视图）

1. **决策面板（默认页）**：今日行动计划（执行/强制/可选三区）+ 风控红灯（违反约束即显示）+ 组合偏离条。
2. **假设与验证**：每条假设的卡片（状态、领先指标、验证进度、校准曲线、证伪条件）——"这个系统为什么相信/不相信什么"一目了然。
3. **组合全景**：目标 vs 实际权重、板块/主题敞口、回撤曲线、现金水平、最近交易。

删除：多板块平铺监测页、逃跑指数、评分拆解等"信号噪声"视图。信号作为"假设卡片"的一部分存在，而不是主角。

---

## 7. AI/Agent 在 v2 中的位置

AI 是**研究助手 + 维护助手**，不是决策者：

- 事件解析：财报/发布会 → 提取结果 → 生成"事件标注补丁"（替代关键词匹配）。
- 指标更新：从公告提取领先指标 value/trend 变化 → 生成 config 补丁，用户确认后写入。
- 复盘归因：自动生成"为什么错/对"分析（区分 beta/alpha、假设层面归因）。
- 回测与敏感性：跑假设的历史模拟，输出校准建议（受门禁约束）。
- 对话式追问："存储板块为什么降权？"——答案从 Edge Registry + 验证记录给出。

红线：AI 产出的任何参数/补丁都需要人工确认；AI 不直接调用交易入口。

---

## 8. 迁移路径（不推倒重来）

v2 可以在现有系统上渐进落地，顺序即优先级：

1. **第 0 步（数据地基）**：修复 record_trade 写路径；storage 层统一（锁+原子写+备份）；portfolio.json schema 化。→ 消除数据丢失风险。
2. **第 1 步（规则单一事实源）**：把 TRADING_CONFIG + position_config + trade_rules 合并为 rules.yaml；现有决策树/叠层信号降级为"研究信号输入"。
3. **第 2 步（Edge Registry + 验证引擎）**：把现有 9 板块的领先指标/周期判断改写为假设登记；预测自动回填；门禁生效。
4. **第 3 步（组合优化器 + 风控覆盖）**：目标权重替代买卖建议；硬约束进优化器；熔断上线。
5. **第 4 步（执行与账本）**：统一交易入口 + override 记录。
6. **第 5 步（UI 收敛）**：三个视图替换现有页面。
7. **第 6 步（AI 助手）**：事件标注、指标补丁、复盘归因。

每一步独立可验证、可回滚；现有 history/predictions 数据直接喂给验证引擎做回放校准。

---

## 9. 对"收益最大化、损失最小化"的最终回答

- **损失最小化靠什么**：仓位集中度硬上限（板块/主题/瓶颈簇）、逻辑止损（证伪即清仓）、回撤熔断、现金底线、拒绝未验证假设。这五条全部是**约束**，不需要预测能力，立刻可做。
- **收益最大化靠什么**：资金流向"已验证假设 × 高置信度"（组合优化器）、再平衡纪律（自动止盈/纪律加仓）、减少损耗（每周决策而非每日、避免赎回费与情绪交易）。这需要验证闭环先转起来。
- **系统不负责的事**：替你预测未来。它负责的是——**把"相信什么"变成"押多少"**，并且只在证据足够时相信。

> v2 的一句话定义：**一个以风险约束为前提、以验证为门禁、以目标权重为输出的个人投资决策操作系统。**
