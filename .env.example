# 个人投资数据目录配置
# 复制本文件为 .env 或 .env.local，并设置真实路径
# 程序通过 config.py 的 DATA_DIR 读取该环境变量

# 个人数据目录：存放 portfolio.json / history / predictions / reviews / portfolio_history / position_snapshots
PERSONAL_DATA_DIR=/home/<you>/personal-investment-data
